# Steel Onboarding Application - Environment Configuration Template
# Copy this file to .env and update values for your production environment

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server port (default: 3001)
PORT=3001

# Node environment (development|production|test)
NODE_ENV=production

# Frontend URL for CORS configuration
FRONTEND_URL=https://onboarding.flawlesssteelwelding.com

# Session timeout in milliseconds (default: 30 minutes)
SESSION_TIMEOUT=1800000

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# JWT Secret - MUST be a strong, randomly generated secret
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-replace-this-with-64-random-hex-chars

# BCrypt rounds for password hashing (10-15 recommended, 12 default)
BCRYPT_ROUNDS=12

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# SQLite database file path (relative to project root)
DB_PATH=./onboarding.db

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================

# SMTP server configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# Email authentication credentials
# For Gmail: use app-specific password, not your regular password
# Enable 2-factor auth and generate app password at: https://myaccount.google.com/apppasswords
EMAIL_USER=your-email@flawlesssteelwelding.com
EMAIL_PASS=your-app-specific-password

# =============================================================================
# NOTIFICATION RECIPIENTS
# =============================================================================

# HR department email for onboarding notifications
HR_EMAIL=hr@flawlesssteelwelding.com

# System administrator email for critical alerts
ADMIN_EMAIL=admin@flawlesssteelwelding.com

# =============================================================================
# OPTIONAL ADVANCED CONFIGURATION
# =============================================================================

# Rate limiting configuration (requests per 15-minute window)
# AUTH_RATE_LIMIT=5
# GENERAL_RATE_LIMIT=100

# Request body size limit for form submissions
# BODY_SIZE_LIMIT=10mb

# Database connection pool settings
# DB_TIMEOUT=30000
# DB_RETRY_ATTEMPTS=3

# File upload limits
# MAX_SIGNATURE_SIZE=5242880

# Logging configuration
# LOG_LEVEL=info
# AUDIT_LOG_RETENTION_DAYS=90

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================

# 1. Security Requirements:
#    - Use HTTPS in production (configure reverse proxy)
#    - Generate strong JWT_SECRET (64+ characters)
#    - Use app-specific passwords for email
#    - Set NODE_ENV=production
#    - Configure firewall to restrict database access

# 2. Performance Optimization:
#    - Set BCRYPT_ROUNDS=12 (balance security/performance)
#    - Configure appropriate rate limits
#    - Monitor memory usage and adjust SESSION_TIMEOUT

# 3. Monitoring & Backup:
#    - Set up automated database backups
#    - Monitor disk space for DB_PATH location
#    - Configure log rotation
#    - Set up health check monitoring

# 4. Email Configuration:
#    - Test email delivery before production
#    - Configure SPF/DKIM for sender domain
#    - Monitor email bounce rates
#    - Set up backup email notification methods

# =============================================================================
# EXAMPLE PRODUCTION VALUES (DO NOT USE AS-IS)
# =============================================================================

# PORT=3001
# NODE_ENV=production
# JWT_SECRET=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456789012345678901234567890abcdef
# BCRYPT_ROUNDS=12
# DB_PATH=/var/lib/steel-onboarding/onboarding.db
# EMAIL_HOST=smtp.office365.com
# EMAIL_PORT=587
# EMAIL_USER=onboarding@flawlesssteelwelding.com
# EMAIL_PASS=app-specific-password-here
# HR_EMAIL=hr@flawlesssteelwelding.com
# ADMIN_EMAIL=admin@flawlesssteelwelding.com
# FRONTEND_URL=https://onboarding.flawlesssteelwelding.com
# SESSION_TIMEOUT=1800000